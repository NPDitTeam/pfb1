<odoo>

    <!-- PDF REPORTS -->
    <!--    Purchase Order-->
    <record id="action_report_purchase_order_print" model="ir.actions.report">
        <field name="name">ใบสั่งซื้อ</field>
        <field name="model">purchase.order</field>
        <field name="report_name">npd_npd_po_qweb.report_po</field>
        <field name="report_file">npd_npd_po_qweb.report_po</field>
        <field name="report_type">qweb-pdf</field>
        <field name="paperformat_id" ref="npd_npd_po_qweb.paperformat_purchase_order"/>
        <field name="print_report_name">'%s%s' % (object.name, object.state == 'draft' and '-draft' or '')
        </field>
        <field name="binding_type">report</field>
    </record>
</odoo>
