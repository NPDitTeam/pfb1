<odoo>
    <record id="popup_shipping_invoice_rule" model="ir.rule">
        <field name="name">Popup Shipping Invoice: Access for Users</field>
        <field name="model_id" ref="model_popup_shipping_invoice"/>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="domain_force">[(1, '=', 1)]</field>
    </record>
</odoo>
